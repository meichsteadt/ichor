export class PortfolioItem {
  constructor(public client: string, public description: string, public review: string, public photo: string, public photoOpen: string, public url: string, public services: {}) { }
}
